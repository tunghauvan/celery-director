example.ETL:
  tasks:
  - EXTRACT
  - TRANSFORM
  - LOAD
  timeout: 60 # 60 seconds

example.RANDOMS:
  tasks:
  - GROUP_RANDOMS:
      tasks:
      - RANDOM
      - RANDOM
      type: group
  - ADD

example.PIPELINE:
  tasks:
  - name: Build
    script: echo "Building..."
